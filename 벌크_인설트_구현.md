# 대량 INSERT를 안정적으로 처리하기 위한 구조를 만들어보자.

<br />
<br />

* 목표는 적은 리소스를 최대한으로 활용하는 것

---

```
완전하고 충분한 개발 환경은 없다.

Redis 도입 전, Kafka 도입 전, RabbitMQ 도입 전
현재 스택으로 해결할 수 있는지 먼저 시도하고
트레이드오프를 따져야 한다.

오버엔지니어링과 오버 스택은 지양한다.
```

<br />
<br />
<br />
<br />

1. 시나리오

```
낮은 사양의 단일 인스턴스 서버.

클라이언트 이벤트로 단기간에 트래픽이 집중된다.

스케일 업 / 스케일 아웃이 당장 어렵다.

[해결 전략]
· 요청을 메모리 버퍼에 모은다 (버퍼링)
· 일정 주기로 DB에 묶어서 반영한다 (배치 INSERT)
· 후속 처리는 별도 Job Queue가 담당한다

* 기본은 단일 인스턴스로 운영하되, 스케일아웃 시에도 처리량이
  파드 수만큼 선형 확장되도록 설계한다 (FOR UPDATE SKIP LOCKED + 배치 병렬)

* 사용 스택 NestJS + Prisma + PostgreSQL
```

<br />
<br />
<br />

2. 전체 흐름

```
[클라이언트]
· POST /api/db-gather

▼
```

```
[QueueController]
· 버퍼에 push만 수행
· 성공 -> 202 accepted
· 버퍼 초과 -> 503 (Backpressure)

▼
```

```
[QueueBuffer (메모리)]
· 0.5초마다 최대 200건을 꺼내 job_queue 테이블에 INSERT
· 틱(tick) 처리 중이면 다음 틱으로 스킵
· 벌크 INSERT 실패 시 단건 fallback
· 단건도 실패 시 flush.log 기록 (재시도 없음)
· 종료 시 버퍼가 빌 때까지 200건 단위 반복 처리

▼
```

```
[job_queue 테이블]
· INSERT 성공 시 DB 트리거가 pg_notify('new_job') 발행

▼
```

```
[JobProcessor (LISTEN 기반)]
· LISTEN new_job 신호 수신
· pending 소진 루프: FOR UPDATE SKIP LOCKED 로 JOB_BATCH_SIZE건씩 획득 후 병렬 처리
· pending 없음 → 루프 탈출 후 다음 LISTEN 신호 대기
· Safety Polling: SAFETY_POLLING_INTERVAL_MS 주기마다 processPending() 강제 실행 (기본값 5분, 신호 누락 대비)
· handle() 성공 -> done
· handle() 실패 -> failed + handle.log 기록

▼
```

```
[정리 스케줄러]
· done 레코드: 24시간 보존 후 자동 삭제 (1시간 주기)
· failed 레코드: 수동 확인 후 삭제
· VACUUM ANALYZE: 매일 새벽 4시
· REINDEX CONCURRENTLY: 매주 일요일 새벽 3시

END
```

<br />

`Backpressure(배압) 의도`

```
· DB 장애가 길어지면 버퍼가 점점 차고, MAX_BUFFER_SIZE 도달 시 503을 반환
· "언젠가 반드시 DB에 적재"하려고 버퍼를 무한히 키우는 구조가 아님
· 장애 시에는 DB 복구가 선행이며, 애플리케이션 레벨의 무한 재시도는 의도적으로 배제
```

<br />
<br />
<br />

3. 왜 이런 구조인가?

| 문제 | 해결 |
| --- | --- |
| 건당 INSERT -> DB 왕복 비용 과다 | 버퍼링 + 배치 INSERT |
| 후속 처리가 요청 타이밍에 집중 | Job Queue 분리 + 배치 병렬 처리 |
| 다중 프로세스 동시 처리 시 중복 | FOR UPDATE SKIP LOCKED |
| polling -> 불필요한 쿼리 발생 | LISTEN/NOTIFY 신호 기반 |
| 벌크 실패 시 전체 유실 위험 | 단건 fallback으로 분리 반영 |
| 실패 원인 추적 어려움 | flush.log / handle.log 분리 기록 |
| 버퍼 무한 증가 위험 | MAX_BUFFER_SIZE 초과 시 503 |
| 단일 인스턴스 처리량 한계 | JOB_BATCH_SIZE건씩 병렬 처리 |
| 스케일아웃 시 처리량 미증가 | SKIP LOCKED + 배치 병렬로 파드 수만큼 선형 확장 |

<br />

`FOR UPDATE SKIP LOCKED — 단일 프로세서 이상을 위한 설계`

```
기본은 단일 인스턴스로 운영하되, 스케일아웃 시에도 효과가 있도록 설계한다.

FOR UPDATE SKIP LOCKED 덕분에 파드가 여러 개여도
동일 job을 두 파드가 동시에 가져가는 중복 처리가 DB 레벨에서 방지된다.

processPending()은 JOB_BATCH_SIZE건을 한 트랜잭션에서 한번에 획득하고
Promise.allSettled로 병렬 처리한다.

· 단일 인스턴스: JOB_BATCH_SIZE건씩 병렬 처리로 처리량 향상
· 스케일아웃:    파드 수 × JOB_BATCH_SIZE 만큼 선형으로 처리량 확장
  (파드 A: job 1~5 획득, 파드 B: job 6~10 획득 — 중복 없음)

단, handle()은 멱등성(idempotency) 보장이 필수다.
(done 업데이트 직전 프로세스가 죽으면, 재기동 시 동일 job이 재처리될 수 있다)
```

<br />
<br />
<br />

4. 트레이드오프 / 리스크 / 배포 전 체크리스트

<br />

`트레이드오프 (의도한 것)`

```
[즉시 202 accepted 응답]
· 클라이언트는 수집 성공으로 인식하지만, 실제 DB 반영은 flush 시점이다.
· 수집 API 특성상 허용되는 구조다.

[flush 단계 재시도 없음]
· DB/네트워크 장애를 버퍼로 무한 흡수하지 않는다.
· 장애가 지속되면 버퍼가 한도까지 차고 503으로 거절된다. (정상 반응)
· 실패 건은 flush.log에만 기록한다. 운영자가 로그로 조치한다.

[단건 fallback의 효과 범위]
· 벌크 INSERT 실패 원인이 DB 연결 장애라면, 단건 fallback도 전부 실패한다.
· 단건 fallback이 실질적으로 효과를 발휘하는 시나리오는
  특정 payload가 DB 제약 조건(유니크, NOT NULL 등)을 위반하는 경우로 한정된다.
· 불량 건만 골라내어 정상 건은 반영하는 것이 목적이며, 재시도 메커니즘이 아니다.

[JobProcessor 배치 병렬 처리]
· JOB_BATCH_SIZE건을 한 트랜잭션에서 획득 후 Promise.allSettled로 병렬 처리한다.
· 일부 실패해도 나머지는 done으로 반영된다 (실패 격리).
· 처리량보다 예측 가능성을 우선하되, 스케일아웃 효과를 위해 배치 병렬을 채택한다.

[트랜잭션 실패 시 Safety Polling 주기만큼 처리 지연]
· processPending() 내 트랜잭션 에러 발생 시 루프를 탈출한다.
· listenClient와 processorPrisma는 별도 커넥션이므로, DB가 복구되어도
  LISTEN 신호가 즉시 재도달하지 않을 수 있다.
· 이 경우 Safety Polling 주기(SAFETY_POLLING_INTERVAL_MS, 기본값 5분)가
  동작할 때까지 최대 해당 주기만큼 pending이 쌓인다.
· 허용 가능한 지연으로 간주하며, 더 짧은 지연이 필요하면
  SAFETY_POLLING_INTERVAL_MS 환경변수로 주기를 단축한다 (재배포 없이 조정 가능).

[스케일아웃 시 connection_limit 주의]
· 파드 수 × JOB_BATCH_SIZE가 QUEUE_PROCESSOR_DATABASE_URL의
  connection_limit 합산을 초과하면 pool_timeout 에러가 발생한다.
· connection_limit은 파드 한 개당 설정값이다.
· 예시) 파드 3개 × JOB_BATCH_SIZE=5 = 총 15 커넥션 필요
        → 파드 한 개당 connection_limit=5 (3개 × 5 = 15, PostgreSQL이 수용 가능한지 확인)
        → 여유분 포함 파드 한 개당 7~8로 설정하는 것을 권장
· handle() Step 1의 contract.upsert()는 공유 풀(DATABASE_URL)을 사용한다.
  배치 병렬 처리 시 JOB_BATCH_SIZE개의 커넥션이 공유 풀에서 동시에 소비된다.
  DATABASE_URL의 connection_limit도 JOB_BATCH_SIZE × 파드 수 이상으로 설정한다.
· QueueBuffer / QueueProcessor / handle() 세 풀의 합이
  PostgreSQL max_connections를 초과하지 않도록 한다.
```

<br />

`리스크 (알고 감수하는 것)`

```
[메모리 버퍼 유실 가능성]
· 비정상 종료(SIGTERM 등)는 onApplicationShutdown에서 flush로 완화한다.
· kill -9 / 하드웨어 장애는 shutdown 훅 자체가 실행되지 않아 버퍼 유실 가능성이 있다.
· WAL 파일로 보완: enqueue 시점에 wal.log에 선기록하고, flush 성공 시 flushed로 표시한다.
  재기동 시 onApplicationBootstrap에서 미처리(pending) 라인을 읽어 버퍼에 복구한다.
· WAL 복구 건수가 MAX_BUFFER_SIZE를 초과하는 경우, 초과분은 버려지고 경고 로그가 남는다.
  초과분이 자주 발생한다면 MAX_BUFFER_SIZE 상향 또는 인스턴스 증설을 검토한다.
· WAL 파일 자체가 손상되는 극단적 상황은 막을 수 없다. (디스크 장애 등)

[스케일아웃 시 WAL 파일 분산]
· WAL 파일은 파드마다 독립적으로 존재한다 (파드 로컬 디스크).
· 파드가 교체되면 이전 파드의 WAL은 복구되지 않는다.
· 허용 가능한 리스크로 간주한다. 빈번한 파드 교체가 예상된다면
  WAL 저장소를 공유 볼륨(PVC)으로 분리하는 것을 검토한다.

[DB/네트워크 장애 시 버퍼 적체]
· 한도(MAX_BUFFER_SIZE) 도달 시 503 반환이 의도된 동작이다.
· 버퍼가 계속 찬다면 DB를 살리는 것이 우선이다.

[외부 API 장애 시 failed 누적]
· 자동 재처리 없음. 수동 확인 프로세스가 필요하다.

[handle() 완료 직전 프로세스 종료]
· done 업데이트 전 종료되면 재기동 시 재처리될 수 있다.
· handle() 구현 시 멱등성(idempotency)이 반드시 보장되어야 한다.

[종료 시 처리 중인 handle() 완료 대기]
· onApplicationShutdown에서 isProcessing이 true인 동안 대기 후 커넥션을 해제한다.
· 대기 없이 Prisma 커넥션을 먼저 닫으면 진행 중인 handle()이 에러로 튈 수 있다.
```

<br />

`배포 전 체크리스트`

```
[flush.log / handle.log 모니터링 체계 수립]
· flush.log에 신규 라인 발생 시 즉시 알림 (정상 운영에서는 발생하지 않아야 함)
· handle.log 누적 건수가 1시간 내 N건 초과 시 알림 (N은 운영 합의)
· tail -f 또는 Filebeat 등으로 실시간 감시 권장

[버퍼 임계치 정책 확정]
· MAX_BUFFER_SIZE 운영값 합의 (기본값: 10,000건 / 환경변수 MAX_BUFFER_SIZE로 외부 주입 가능)
· 설정 전 건당 평균 payload 크기를 측정한다.
  (건수 × 평균 크기)가 Node.js --max-old-space-size의 10~20% 이내인지 확인한다.
  예시) payload 평균 1KB × 10,000건 = 약 10MB -> 안전
       payload 평균 50KB × 10,000건 = 약 500MB -> MAX_BUFFER_SIZE 축소 필요
· 503 반환 시 클라이언트 재시도 정책 합의
  예시) 지수 백오프(Exponential Backoff) + 최대 재시도 횟수 설정

[서버 시간존 설정 확인]
· scheduleDailyAt / scheduleWeeklyAt은 서버 로컬 시간 기준으로 동작한다.
· 배포 환경에서 TZ 환경변수가 의도한 시간대로 설정되어 있는지 반드시 확인한다.
  예시) TZ=Asia/Seoul
· TZ 미설정 시 UTC 기준으로 동작하여 VACUUM/REINDEX 실행 시각이 의도와 달라질 수 있다.

[handle() 멱등성 검증]
· 동일 payload로 두 번 호출되어도 부작용이 없는지 확인
  예시) 외부 API 호출 전 중복 확인, upsert 사용, 고유 키 방어 등
· 재기동 시 processing -> pending 복구 로직 동작 확인 (onApplicationBootstrap)

[LISTEN 재연결 완전 실패 시 대응]
· MAX_RECONNECT_RETRY 소진 시 process.exit(1)로 프로세스가 종료됨
· PM2 / systemd 등 프로세스 매니저의 자동 재기동 설정이 반드시 되어 있어야 함
  PM2 예시) pm2 start dist/main.js --restart-delay=3000
· 재기동 시 onApplicationBootstrap에서 processing -> pending 자동 복구됨
· 재연결 소진 시점에 Slack 등 알림 발송 연동 권장

[로컬 개발 환경 주의]
· QUEUE_PROCESSOR_DATABASE_URL 미설정 시 DATABASE_URL로 fallback됨
· 두 URL이 동일하면 커넥션 풀 분리 효과가 없다.
· 로컬에서도 가능하면 별도 URL을 설정하거나, 분리 효과가 없음을 인지하고 사용한다.

[SQL 적용 경로 확정 (6절 참고)]
· setup.sql 단독 vs Prisma migrate + 트리거 별도 적용 중 하나만 선택
· 이중 적용 시 인덱스 충돌 발생 가능

[환경변수 확인]
· TZ 설정 여부 확인 (미설정 시 UTC 기준 동작 — VACUUM/REINDEX 시각 의도와 달라질 수 있음)
· EXTERNAL_API_URL 설정 여부 확인 (미설정 시 handle() Step 2에서 빈 URL로 요청됨)
· QUEUE_PROCESSOR_DATABASE_URL 설정 여부 확인 (미설정 시 풀 분리 효과 없음)
· SAFETY_POLLING_INTERVAL_MS 설정 여부 확인 (미설정 시 기본값 300000ms = 5분으로 동작)
· JOB_BATCH_SIZE 설정 여부 확인 (미설정 시 기본값 5로 동작)
  파드 수 × JOB_BATCH_SIZE가 QUEUE_PROCESSOR_DATABASE_URL의 connection_limit 이하인지 확인
· KUBERNETES_MODE 설정 여부 확인 (미설정 시 loadFromDefault() 동작 — 로컬 개발 전용)
```

<br />
<br />
<br />

5. 파일 구조

```text
src/
├── common/
│   ├── app-logger.ts             # 커스텀 Logger (콘솔 + app.log 동시 출력)
│   ├── append-json-log.ts        # 날짜별 로그 파일 append 유틸
│   └── wal-log.ts                # WAL 파일 read/write/mark 유틸 (버퍼 유실 복구용)
├── monitor/
│   ├── monitor.controller.ts     # /monitor/nodes, /monitor/pods, /monitor/queue
│   ├── monitor.service.ts        # K8s Metrics API + job_queue 통계
│   └── monitor.module.ts
├── queue/
│   ├── dto/
│   │   └── enqueue.dto.ts        # 요청 DTO
│   ├── queue.controller.ts       # HTTP 엔드포인트
│   ├── queue.buffer.ts           # 메모리 버퍼 + 배치 flush 담당
│   ├── queue.processor.ts        # LISTEN/NOTIFY 기반 Job 처리
│   └── queue.module.ts
├── prisma.service.ts             # PrismaClient 래퍼 (기존 파일 유지 + JobQueue 모델 추가)
├── app.module.ts
└── main.ts
prisma/
└── schema.prisma
sql/
└── setup.sql
k8s/
└── monitor-rbac.yaml             # K8s RBAC (모니터링용 ServiceAccount)
public/
└── dashboard.html                # 정적 단일 파일 대시보드 (10초 자동 갱신)
logs/
└── YYYY/MM/DD/
    ├── app.log                   # 전체 서버 로그 (log / warn / error)
    ├── flush.log                 # 배치 INSERT 최종 실패 건
    ├── handle.log                # handle() 실패 건
└── wal.log                       # enqueue 선기록 (재기동 시 버퍼 복구용) — 날짜별 아닌 고정 경로
```

<br />
<br />
<br />

6. SQL 설정


```
적용 경로 (반드시 하나만 선택)

A) [setup.sql 전체 적용 (테이블 + 인덱스 + 트리거)]
· prisma generate 만 실행 (migrate dev 실행 금지 — 중복 생성)

B) [Prisma migrate dev 적용 (테이블 + 인덱스)]
· setup.sql의 CREATE TABLE / CREATE INDEX 는 실행하지 않음
· notify_new_job 트리거 블록만 psql 등으로 별도 실행
· migrate dev 만으로는 트리거가 생성되지 않음

* 두 경로를 동시에 적용하면 인덱스 이름 충돌 또는 중복 인덱스가 생성된다.
```

<br />

`sql/setup.sql`

```sql
-- job_queue 테이블
CREATE TABLE job_queue (
  id           SERIAL PRIMARY KEY,
  type         VARCHAR     NOT NULL,
  payload      JSONB       NOT NULL,
  status       VARCHAR     NOT NULL DEFAULT 'pending',
  error        TEXT,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  processed_at TIMESTAMPTZ
);

-- 상태 + 생성일 복합 인덱스 (pending 조회 성능)
CREATE INDEX idx_job_queue_status_created
  ON job_queue (status, created_at);

-- INSERT 시 LISTEN/NOTIFY 신호를 발행하는 트리거 함수
CREATE OR REPLACE FUNCTION notify_new_job()
RETURNS trigger AS $$
BEGIN
  PERFORM pg_notify('new_job', 'batch');
  RETURN NULL;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS trg_notify_new_job ON job_queue;

-- statement 단위 트리거: 벌크 INSERT여도 신호는 1회만 발행
CREATE TRIGGER trg_notify_new_job
AFTER INSERT ON job_queue
FOR EACH STATEMENT EXECUTE FUNCTION notify_new_job();
```

<br />
<br />
<br />

7. Prisma 설정

<br />

`prisma/schema.prisma`

```prisma
model JobQueue {
  id          Int       @id @default(autoincrement())
  type        String
  payload     Json
  status      String    @default("pending")
  error       String?
  createdAt   DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)
  processedAt DateTime? @map("processed_at") @db.Timestamptz(3)

  @@index([status, createdAt])
  @@map("job_queue")
}
```

```
기존 `schema.prisma`의 `generator` · `datasource`는 유지하고, `JobQueue` 모델 블록만 추가한다.

setup.sql을 이미 적용했다면 `prisma generate` 만 실행한다.
```

<br />

`handle() Step 1에서 사용하는 Contract 모델`

```prisma
// handle()의 upsert 대상 테이블.
// contractNo를 고유 키로 사용해 멱등성을 보장한다.
// 실제 비즈니스 스펙에 맞게 필드를 확장한다.
model Contract {
  id          Int      @id @default(autoincrement())
  contractNo  String   @unique @map("contract_no")
  dbCode      String   @map("db_code")
  data        Json
  createdAt   DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt   DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@map("contract")
}
```

```
· contractNo에 @unique 제약이 있어야 upsert의 where 조건으로 사용할 수 있다.
· handle()에서 this.prisma.contract.upsert()를 호출하므로
  이 모델이 schema에 없으면 Prisma 타입 에러가 발생한다.
· migrate dev 실행 시 contract 테이블도 함께 생성된다.
```

<br />

`Connection Pool 설정`

```
Prisma의 기본 `connection_limit`은 `cpu 코어 수 × 2 + 1`로 자동 계산된다.

저사양 서버(1~2 core)면 풀이 3~5개밖에 안 되는데, `QueueBuffer`가 배치 INSERT로
커넥션을 점유하는 동안 `QueueProcessor`가 커넥션을 못 얻어 pending 처리가 밀릴 수 있다.

풀을 명시적으로 설정하고, Buffer용과 Processor용을 분리한다.

[풀 사용처 정리]
· DATABASE_URL (공유 풀)
  - QueueBuffer: 배치 INSERT
  - handle() Step 1: contract.upsert()
  - cleanupDone / VACUUM / REINDEX: 유지보수
  → 배치 병렬 처리 시 JOB_BATCH_SIZE × 파드 수만큼 동시 소비됨
    DATABASE_URL의 connection_limit을 이 값 이상으로 설정한다.

· QUEUE_PROCESSOR_DATABASE_URL (분리 풀)
  - processPending(): job 획득 트랜잭션 + done/failed 업데이트
  → connection_limit은 JOB_BATCH_SIZE × 파드 수 이상으로 설정한다.

· 두 풀의 합이 PostgreSQL max_connections를 초과하지 않도록 한다.
  max_connections 확인: SHOW max_connections;

스케일아웃 시에는 파드 수 × JOB_BATCH_SIZE가
각 풀의 connection_limit 이하인지 반드시 확인한다.
```

<br />

`.env`

```dotenv
# 서버 시간존 — scheduleDailyAt / scheduleWeeklyAt이 로컬 시간 기준으로 동작하므로 반드시 설정
TZ=Asia/Seoul

# QueueBuffer (배치 INSERT) — 트래픽 폭주 시 메인 풀 소비
DATABASE_URL="postgresql://user:pass@host:5432/db?connection_limit=10&pool_timeout=10"

# QueueProcessor (Job 처리) — Buffer와 풀을 완전히 분리
# 로컬 개발 시 미설정이면 DATABASE_URL로 fallback — 풀 분리 효과 없음 (인지 후 사용)
# 스케일아웃 시: 파드 수 × JOB_BATCH_SIZE가 이 값 이하인지 확인
QUEUE_PROCESSOR_DATABASE_URL="postgresql://user:pass@host:5432/db?connection_limit=5&pool_timeout=5"

# 버퍼 임계치 (기본값: 10000) — 재배포 없이 조정 가능
MAX_BUFFER_SIZE=10000

# Safety Polling 주기 ms (기본값: 300000 = 5분) — 재배포 없이 조정 가능
# LISTEN 신호 누락 시 최대 이 주기만큼 pending 처리가 지연될 수 있다
# 더 빠른 복구가 필요하면 값을 줄인다 (예: 60000 = 1분)
SAFETY_POLLING_INTERVAL_MS=300000

# 한 번에 획득하여 병렬 처리할 job 수 (기본값: 5)
# · 단일 인스턴스: 5~10 권장
# · 스케일아웃:    파드 수 × 이 값이 connection_limit 이하인지 확인
#   예시) 파드 3개 × 5 = 15 커넥션 → connection_limit=5 이상 (여유분 포함 7~8 권장)
JOB_BATCH_SIZE=5

# handle()에서 전송할 외부 API 엔드포인트
EXTERNAL_API_URL="https://external-api.example.com/endpoint"

# kubeconfig 로딩 방식 결정 (모니터링 대시보드)
# · 'cluster' : 파드 내부 배포 시 — ServiceAccount 토큰 자동 로딩 (운영)
# · 미설정 또는 그 외 값 : 로컬 개발 시 — ~/.kube/config 로딩
KUBERNETES_MODE=cluster
```

```
[connection_limit 설정 기준 (예시)]
· QueueBuffer: API 동시 요청 수와 flush 주기를 고려해 설정
  트래픽이 많을수록 배치 INSERT가 자주 발생하므로 여유 있게 잡는다.
· QueueProcessor: JOB_BATCH_SIZE × 파드 수 이상으로 설정한다.
  (job 획득 트랜잭션 + 병렬 handle() + VACUUM/REINDEX 등 유지보수 용도)
· 두 풀의 합이 PostgreSQL max_connections를 초과하지 않도록 한다.
  max_connections 확인: SHOW max_connections;
```

<br />

`prisma.service.ts`

```ts
import { Injectable, OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

/**
 * 애플리케이션 전역 PrismaClient 래퍼.
 * QueueBuffer의 배치 INSERT 및 cleanupDone / VACUUM / REINDEX 등 유지보수 용도로 사용된다.
 * QueueProcessor의 Job 처리(processPending)는 전용 커넥션 풀 분리를 위해
 * onApplicationBootstrap에서 별도 PrismaClient를 직접 생성한다.
 */
@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

```
· 기존 프로젝트에 PrismaService가 이미 있다면 이 파일은 그대로 유지한다.
· QueueModule에서 주입받아 사용하므로 DatabaseModule 또는 AppModule에서
  전역(global) 또는 exports로 제공되어야 한다.
· QueueProcessor가 직접 new PrismaClient()를 하는 것은 풀 분리가 목적이며,
  PrismaService와 중복이 아니다.
```

<br />

```bash
npm install pg class-validator class-transformer @kubernetes/client-node
npm install -D @types/pg

npx prisma migrate dev   # setup.sql 미적용 시에만
npx prisma generate
```

<br />
<br />
<br />

8. 코드

```
[구현 원칙]
· 0.5초 타이머마다 최대 200건만 DB 반영 (enqueue는 push만)
· 종료 시 버퍼가 빌 때까지 200건 단위 반복 처리
· 벌크 INSERT 실패 시 단건 fallback (불량 건 분리 목적, 재시도 아님)
· 단건도 실패한 건만 flush.log 기록
· JOB_BATCH_SIZE건을 한 트랜잭션에서 획득 후 Promise.allSettled로 병렬 처리
· 중첩 try/catch 최소화
· Promise.race 미사용 (AbortController로 타임아웃 처리)
```

<br />

`main.ts`

```ts
import { ValidationPipe } from '@nestjs/common';
import { NestFactory } from '@nestjs/core';
import { NestExpressApplication } from '@nestjs/platform-express';
import { join } from 'path';
import { AppModule } from './app.module';
import { AppLogger } from './common/app-logger';

async function bootstrap() {
  const app = await NestFactory.create<NestExpressApplication>(AppModule, {
    logger: new AppLogger(),
  });

  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      forbidNonWhitelisted: true,
      transform: true,
    }),
  );

  // public/ 디렉터리를 정적 파일로 서빙
  // 브라우저에서 http://<host>:<port>/dashboard/dashboard.html 로 접근
  app.useStaticAssets(join(__dirname, '..', 'public'), { prefix: '/dashboard' });

  app.enableShutdownHooks();
  await app.listen(process.env.PORT ?? 4000);
}
bootstrap();
```

<br />

`queue/dto/enqueue.dto.ts`

```ts
import { IsNotEmpty, IsObject, IsString } from 'class-validator';

export class EnqueueDto {
  @IsString()
  @IsNotEmpty()
  dbCode: string;

  @IsString()
  @IsNotEmpty()
  contractNo: string;

  @IsObject()
  payload: Record<string, unknown>;
}
```

<br />

`queue/queue.controller.ts`

```ts
import {
  Body,
  Controller,
  HttpCode,
  Post,
  ServiceUnavailableException,
} from '@nestjs/common';
import { EnqueueDto } from './dto/enqueue.dto';
import { QueueBuffer } from './queue.buffer';

@Controller('api')
export class QueueController {
  constructor(private readonly queueBuffer: QueueBuffer) {}

  @Post('db-gather')
  @HttpCode(202)
  async receive(@Body() dto: EnqueueDto) {
    const accepted = await this.queueBuffer.enqueue(dto);
    if (!accepted) {
      throw new ServiceUnavailableException('buffer is full, try again later');
    }
    return { status: 'accepted' };
  }
}
```

<br />

`queue/queue.buffer.ts`

```ts
import {
  Injectable,
  OnApplicationBootstrap,
  OnApplicationShutdown,
} from '@nestjs/common';
import { PrismaService } from '../prisma.service';
import { EnqueueDto } from './dto/enqueue.dto';
import { AppLogger } from '../common/app-logger';
import { appendJsonLog } from '../common/append-json-log';
import { walAppend, walMarkFlushed, walReadPending, walCleanup } from '../common/wal-log';

@Injectable()
export class QueueBuffer implements OnApplicationBootstrap, OnApplicationShutdown {
  private readonly FLUSH_INTERVAL_MS = 500;
  private readonly BATCH_SIZE = 200;
  private readonly MAX_BUFFER_SIZE =
    Number(process.env.MAX_BUFFER_SIZE) || 10_000;

  private readonly buffer: Array<{ walId: string; dto: EnqueueDto }> = [];
  private timer?: ReturnType<typeof setInterval>;
  private isFlushing = false;
  private flushingDone: Promise<void> = Promise.resolve();

  private readonly logger = new AppLogger(QueueBuffer.name);

  constructor(private readonly prisma: PrismaService) {}

  async onApplicationBootstrap() {
    try {
      const pending = await walReadPending();
      if (pending.length > 0) {
        this.logger.warn(`WAL 복구: ${pending.length}건을 버퍼에 재적재합니다.`);
        for (const { walId, dto } of pending) {
          if (this.buffer.length >= this.MAX_BUFFER_SIZE) {
            this.logger.warn(
              `WAL 복구 중 버퍼 한도(${this.MAX_BUFFER_SIZE}) 초과 — 나머지 ${pending.length - this.buffer.length}건을 버립니다.`,
            );
            break;
          }
          this.buffer.push({ walId, dto });
        }
      }
    } catch (e) {
      this.logger.error('WAL 복구 실패', e instanceof Error ? e.stack : String(e));
    }

    this.timer = setInterval(() => void this.flushChunk(), this.FLUSH_INTERVAL_MS);
  }

  async onApplicationShutdown() {
    if (this.timer) clearInterval(this.timer);
    if (this.isFlushing) await this.flushingDone;
    while (this.buffer.length > 0) {
      await this.flushChunk();
    }
    try {
      await walCleanup();
    } catch (e) {
      this.logger.error('WAL 정리 실패', e instanceof Error ? e.stack : String(e));
    }
  }

  async enqueue(dto: EnqueueDto): Promise<boolean> {
    if (this.buffer.length >= this.MAX_BUFFER_SIZE) return false;

    let walId = '';
    try {
      walId = await walAppend(dto);
    } catch (e) {
      this.logger.warn(`WAL 선기록 실패 (버퍼 추가는 계속 진행): ${String(e)}`);
    }

    this.buffer.push({ walId, dto });
    return true;
  }

  private async flushChunk(): Promise<void> {
    if (this.isFlushing || this.buffer.length === 0) return;

    this.isFlushing = true;

    let resolveFlushing!: () => void;
    this.flushingDone = new Promise<void>((resolve) => {
      resolveFlushing = resolve;
    });

    try {
      const chunk = this.buffer.splice(0, this.BATCH_SIZE);
      const successWalIds: string[] = [];

      try {
        await this.prisma.jobQueue.createMany({
          data: chunk.map(({ dto }) => ({
            type: 'DB_GATHER',
            payload: dto,
            status: 'pending',
          })),
        });
        successWalIds.push(...chunk.map(({ walId }) => walId).filter(Boolean));
      } catch (bulkError) {
        this.logger.warn(`벌크 INSERT 실패, 단건 fallback 시작 (${chunk.length}건): ${String(bulkError)}`);
        for (const { walId, dto } of chunk) {
          try {
            await this.prisma.jobQueue.create({
              data: { type: 'DB_GATHER', payload: dto, status: 'pending' },
            });
            if (walId) successWalIds.push(walId);
          } catch (singleError) {
            await this.safeWriteFlushLog({
              context: 'QueueBuffer',
              dto,
              bulkError: String(bulkError),
              singleError: String(singleError),
              timestamp: new Date().toISOString(),
            });
          }
        }
      }

      if (successWalIds.length > 0) {
        try {
          await walMarkFlushed(successWalIds);
        } catch (e) {
          this.logger.warn(`WAL flushed 표시 실패 (재기동 시 중복 복구 가능): ${String(e)}`);
        }
      }
    } catch (e) {
      this.logger.error('flush 예외 발생', e instanceof Error ? e.stack : String(e));
    } finally {
      this.isFlushing = false;
      resolveFlushing();
    }
  }

  private async safeWriteFlushLog(payload: Record<string, unknown>) {
    try {
      await appendJsonLog('flush.log', payload);
    } catch (e) {
      this.logger.error('flush.log 기록 실패', `${String(e)} / payload: ${JSON.stringify(payload)}`);
    }
  }
}
```

<br />

`queue/queue.processor.ts`

```ts
import {
  Injectable,
  OnApplicationBootstrap,
  OnApplicationShutdown,
} from '@nestjs/common';
import { PrismaClient } from '@prisma/client';
import { PrismaService } from '../prisma.service';
import { Client } from 'pg';
import { AppLogger } from '../common/app-logger';
import { appendJsonLog } from '../common/append-json-log';

interface JobPayload {
  dbCode: string;
  contractNo: string;
  payload: Record<string, unknown>;
}

@Injectable()
export class QueueProcessor implements OnApplicationBootstrap, OnApplicationShutdown {
  private readonly CLEANUP_INTERVAL_MS = 60 * 60 * 1000;
  private readonly RETENTION_MS = 24 * 60 * 60 * 1000;
  private readonly MAX_RECONNECT_RETRY = 5;
  private readonly SAFETY_POLLING_INTERVAL_MS =
    Number(process.env.SAFETY_POLLING_INTERVAL_MS) || 5 * 60 * 1000;

  // 한 번에 획득하여 병렬 처리할 job 수
  // · 단일 인스턴스: 5~10 권장
  // · 스케일아웃: 파드 수 × 이 값이 connection_limit 이하인지 확인
  private readonly JOB_BATCH_SIZE =
    Number(process.env.JOB_BATCH_SIZE) || 5;

  private processorPrisma!: PrismaClient;
  private listenClient?: Client;
  private cleanupTimer?: ReturnType<typeof setInterval>;
  private safetyPollingTimer?: ReturnType<typeof setInterval>;
  private vacuumTimer?: ReturnType<typeof setTimeout>;
  private reindexTimer?: ReturnType<typeof setTimeout>;
  private reconnectTimer?: ReturnType<typeof setTimeout>;

  private isReconnecting = false;
  private isShuttingDown = false;
  private isProcessing = false;
  private processingDone: Promise<void> = Promise.resolve();

  private readonly logger = new AppLogger(QueueProcessor.name);

  constructor(private readonly prisma: PrismaService) {}

  async onApplicationBootstrap() {
    this.processorPrisma = new PrismaClient({
      datasources: {
        db: {
          url: process.env.QUEUE_PROCESSOR_DATABASE_URL ?? process.env.DATABASE_URL,
        },
      },
    });
    // 명시적 연결: 첫 쿼리 전에 연결 실패를 bootstrap 시점에 잡는다.
    await this.processorPrisma.$connect();

    // 비정상 종료로 processing 상태에 걸린 job을 pending으로 복구
    await this.processorPrisma.jobQueue.updateMany({
      where: { status: 'processing' },
      data: { status: 'pending' },
    });

    await this.connectListen();
    await this.processPending();

    this.cleanupTimer = setInterval(() => void this.cleanupDone(), this.CLEANUP_INTERVAL_MS);
    this.safetyPollingTimer = setInterval(
      () => void this.processPending(),
      this.SAFETY_POLLING_INTERVAL_MS,
    );
    this.vacuumTimer = this.scheduleDailyAt(4, 0, () => void this.runVacuumAnalyze());
    this.reindexTimer = this.scheduleWeeklyAt(0, 3, 0, () => void this.runReindexConcurrently());
  }

  async onApplicationShutdown() {
    this.isShuttingDown = true;
    clearInterval(this.cleanupTimer);
    clearInterval(this.safetyPollingTimer);
    clearTimeout(this.vacuumTimer);
    clearTimeout(this.reindexTimer);
    clearTimeout(this.reconnectTimer);

    if (this.isProcessing) await this.processingDone;

    if (this.listenClient) {
      this.listenClient.removeAllListeners();
      await this.listenClient.end();
    }

    if (this.processorPrisma) await this.processorPrisma.$disconnect();
  }

  // ─── LISTEN/NOTIFY ──────────────────────────────────────────────────────────

  private async connectListen() {
    this.listenClient = new Client({ connectionString: process.env.DATABASE_URL });
    await this.listenClient.connect();
    await this.listenClient.query('LISTEN new_job');

    this.listenClient.on('notification', () => void this.processPending());
    this.listenClient.on('error', (err) => {
      this.logger.warn(`LISTEN 에러: ${err?.message ?? err}`);
      void this.reconnect();
    });
    this.listenClient.on('end', () => {
      this.logger.warn('LISTEN 커넥션 종료됨');
      void this.reconnect();
    });
  }

  private async reconnect() {
    if (this.isReconnecting || this.isShuttingDown) return;
    this.isReconnecting = true;

    if (this.listenClient) {
      this.listenClient.removeAllListeners();
      await this.listenClient
        .end()
        .catch((e) => this.logger.warn(`기존 커넥션 종료 중 무시: ${e?.message}`));
    }

    let reconnected = false;
    try {
      for (let attempt = 1; attempt <= this.MAX_RECONNECT_RETRY; attempt++) {
        if (this.isShuttingDown) break;

        await new Promise<void>((resolve) => {
          this.reconnectTimer = setTimeout(resolve, attempt * 1000);
        });

        try {
          await this.connectListen();
          await this.processPending();
          reconnected = true;
          break;
        } catch (e) {
          this.logger.error(`재연결 실패 (${attempt}/${this.MAX_RECONNECT_RETRY})`, String(e));
        }
      }

      if (!reconnected && !this.isShuttingDown) {
        // TODO: 이 시점에 Slack 등 알림 발송 연동 권장
        this.logger.error(
          `LISTEN 재연결 ${this.MAX_RECONNECT_RETRY}회 전부 실패. 프로세스를 종료합니다.`,
        );
        process.exit(1);
      }
    } finally {
      // reconnect() 자체가 예외로 종료되어도 isReconnecting을 반드시 해제한다.
      // 해제되지 않으면 이후 error/end 이벤트에서 재연결 시도가 영구 차단된다.
      this.isReconnecting = false;
    }
  }

  // ─── Job 처리 ────────────────────────────────────────────────────────────────

  /**
   * pending job을 JOB_BATCH_SIZE건씩 획득해 병렬 처리한다.
   *
   * [단일 인스턴스]
   * · JOB_BATCH_SIZE건을 한 트랜잭션에서 획득 후 Promise.allSettled로 병렬 처리.
   * · 1건씩 순차 처리 대비 처리량 향상.
   *
   * [스케일아웃 시]
   * · FOR UPDATE SKIP LOCKED: 파드 A가 job 1~5를 잡으면 파드 B는 자동으로 job 6~10을 가져감.
   * · 중복 처리 없이 파드 수만큼 처리량이 선형 확장됨.
   * · 파드 수 × JOB_BATCH_SIZE가 connection_limit을 초과하지 않도록 주의.
   *
   * [실패 격리]
   * · Promise.allSettled로 일부 실패해도 나머지는 done으로 반영됨.
   * · 배치 내 한 건의 실패가 전체에 영향을 주지 않음.
   */
  private async processPending() {
    if (this.isShuttingDown || this.isProcessing) return;
    this.isProcessing = true;

    let resolveProcessing!: () => void;
    this.processingDone = new Promise<void>((resolve) => {
      resolveProcessing = resolve;
    });

    try {
      while (true) {
        if (this.isShuttingDown) break;

        try {
          // ── 1. JOB_BATCH_SIZE건을 한 트랜잭션에서 한번에 획득 ──────────────
          const jobs = await this.processorPrisma.$transaction(
            async (tx) => {
              // $queryRaw 템플릿 리터럴 변수는 Prisma가 파라미터 바인딩($1)으로 처리한다.
              // LIMIT $1 은 PostgreSQL에서 정상 동작하므로 $executeRawUnsafe 불필요.
              const rows = await tx.$queryRaw<{ id: number; payload: unknown }[]>`
                SELECT id, payload
                FROM job_queue
                WHERE status = 'pending'
                ORDER BY created_at ASC
                LIMIT ${this.JOB_BATCH_SIZE}
                FOR UPDATE SKIP LOCKED
              `;

              if (rows.length === 0) return [];

              const ids = rows.map((r) => r.id);
              await tx.jobQueue.updateMany({
                where: { id: { in: ids } },
                data: { status: 'processing' },
              });

              return rows;
            },
            { timeout: 10_000 },
          );

          // pending 소진 → 루프 탈출 후 다음 LISTEN 신호 대기
          if (jobs.length === 0) break;

          // ── 2. 획득한 N건을 Promise.allSettled로 병렬 처리 ─────────────────
          const results = await Promise.allSettled(
            jobs.map((job) => this.handle(job.payload as JobPayload)),
          );

          // ── 3. 결과 반영 — 성공/실패 분리해서 업데이트 ───────────────────────
          const doneIds: number[] = [];
          const failedJobs: { id: number; error: string }[] = [];

          results.forEach((result, i) => {
            if (result.status === 'fulfilled') {
              doneIds.push(jobs[i].id);
            } else {
              failedJobs.push({
                id: jobs[i].id,
                error: result.reason instanceof Error
                  ? result.reason.message
                  : String(result.reason),
              });
            }
          });

          // done 일괄 업데이트
          if (doneIds.length > 0) {
            await this.processorPrisma.jobQueue.updateMany({
              where: { id: { in: doneIds } },
              data: { status: 'done', processedAt: new Date() },
            });
          }

          // failed 개별 업데이트 (error 메시지가 건마다 다름)
          await Promise.allSettled(
            failedJobs.map(({ id, error }) =>
              this.processorPrisma.jobQueue.update({
                where: { id },
                data: { status: 'failed', error, processedAt: new Date() },
              }),
            ),
          );

          // handle.log 기록
          await Promise.allSettled(
            failedJobs.map(({ id, error }) => {
              const job = jobs.find((j) => j.id === id)!;
              return this.safeWriteHandleLog({
                context: 'QueueProcessor',
                jobId: id,
                payload: job.payload,
                error,
                timestamp: new Date().toISOString(),
              });
            }),
          );

        } catch (transactionError) {
          this.logger.error('트랜잭션 오류', String(transactionError));
          await new Promise((resolve) => setTimeout(resolve, 1000));
          break;
        }
      }
    } finally {
      this.isProcessing = false;
      resolveProcessing();
    }
  }

  // ─── 비즈니스 로직 ───────────────────────────────────────────────────────────

  /**
   * 실제 비즈니스 처리 로직.
   *
   * ① 멱등성(idempotency) 보장
   *    done 업데이트 전 프로세스가 종료되면 재기동 시 동일 job이 재처리될 수 있다.
   *    스케일아웃 시에도 SKIP LOCKED로 중복 획득은 방지되나,
   *    handle() 자체는 항상 멱등성을 보장해야 한다.
   *    → upsert로 동일 contractNo가 두 번 들어와도 중복 행 없이 최신 값으로 덮어쓴다.
   *
   * ② 외부 API 타임아웃 (AbortController)
   *    handle()이 hang되면 해당 배치 슬롯이 막힌다.
   *    Promise.race는 타임아웃 후에도 원본 Promise가 백그라운드에서 계속 실행되므로 사용하지 않는다.
   */
  private async handle(payload: JobPayload): Promise<void> {
    const TIMEOUT_MS = 10_000;

    // ── Step 1. 수신 데이터를 실제 DB 테이블에 upsert ────────────────────────
    // this.prisma(공유 풀)를 사용한다.
    // 배치 병렬 처리 시 JOB_BATCH_SIZE개의 handle()이 동시에 공유 풀 커넥션을 사용하므로
    // DATABASE_URL의 connection_limit이 JOB_BATCH_SIZE × 파드 수 이상인지 확인한다.
    await this.prisma.contract.upsert({
      where: { contractNo: payload.contractNo },
      update: {
        dbCode: payload.dbCode,
        data: payload.payload,
        updatedAt: new Date(),
      },
      create: {
        contractNo: payload.contractNo,
        dbCode: payload.dbCode,
        data: payload.payload,
      },
    });

    // ── Step 2. 외부 API로 전송 ───────────────────────────────────────────────
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), TIMEOUT_MS);

    try {
      const res = await fetch(process.env.EXTERNAL_API_URL ?? '', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          contractNo: payload.contractNo,
          dbCode: payload.dbCode,
          payload: payload.payload,
        }),
        signal: controller.signal,
      });

      if (!res.ok) {
        throw new Error(`외부 API 오류: ${res.status} ${res.statusText}`);
      }
    } finally {
      clearTimeout(timeoutId);
    }
  }

  // ─── 정리 / 유지보수 ─────────────────────────────────────────────────────────

  private async cleanupDone() {
    // 스케일아웃 시 파드마다 중복 실행되지 않도록 advisory lock으로 단일 실행 보장.
    // deleteMany는 중복 실행 시 데이터 손상은 없지만 불필요한 DB 부하를 줄인다.
    // listenClient가 재연결 중이면 스킵 — 다음 주기(1시간 후)에 재시도.
    if (!this.listenClient || this.isReconnecting) return;

    let lockAcquired = false;
    try {
      const result = await this.listenClient.query<{ pg_try_advisory_lock: boolean }>(
        'SELECT pg_try_advisory_lock(12347)',
      );
      lockAcquired = result.rows[0].pg_try_advisory_lock;
    } catch (e) {
      // 잠금 실패 시 스킵 — 다음 주기(1시간 후)에 재시도
    }

    if (!lockAcquired) return;

    try {
      const cutoff = new Date(Date.now() - this.RETENTION_MS);
      await this.prisma.jobQueue.deleteMany({
        where: { status: 'done', createdAt: { lt: cutoff } },
      });
    } catch (e) {
      this.logger.error('[cleanup] done 레코드 정리 실패', String(e));
    } finally {
      await this.listenClient.query('SELECT pg_advisory_unlock(12347)').catch(() => {});
    }
  }

  private async runVacuumAnalyze() {
    // 스케일아웃 시 파드마다 실행되는 것을 방지하기 위해 pg_try_advisory_lock으로 리더 선출.
    // listenClient(pg.Client)를 사용해 잠금/해제를 동일 세션에서 수행한다.
    // $executeRawUnsafe는 풀에서 임의 커넥션을 꺼내므로 세션이 달라 해제가 보장되지 않는다.
    // 단일 인스턴스 환경에서도 동일하게 동작한다 (항상 획득 성공).
    // listenClient가 재연결 중이면 스킵 — 다음 스케줄에 재시도.
    if (!this.listenClient || this.isReconnecting) {
      if (!this.isShuttingDown) {
        this.vacuumTimer = this.scheduleDailyAt(4, 0, () => void this.runVacuumAnalyze());
      }
      return;
    }

    let lockAcquired = false;
    try {
      const result = await this.listenClient.query<{ pg_try_advisory_lock: boolean }>(
        'SELECT pg_try_advisory_lock(12345)',
      );
      lockAcquired = result.rows[0].pg_try_advisory_lock;
    } catch (e) {
      this.logger.warn('[maintenance] advisory lock 확인 실패, VACUUM 스킵');
    }

    if (!lockAcquired) {
      this.logger.log('[maintenance] VACUUM ANALYZE 스킵 (다른 파드가 실행 중)');
      if (!this.isShuttingDown) {
        this.vacuumTimer = this.scheduleDailyAt(4, 0, () => void this.runVacuumAnalyze());
      }
      return;
    }

    try {
      this.logger.log('[maintenance] VACUUM ANALYZE 시작');
      await this.prisma.$executeRawUnsafe('VACUUM ANALYZE job_queue');
      this.logger.log('[maintenance] VACUUM ANALYZE 완료');
    } catch (e) {
      this.logger.error('[maintenance] VACUUM ANALYZE 실패', String(e));
    } finally {
      // 잠금을 획득한 것과 동일한 listenClient 세션에서 해제한다.
      await this.listenClient.query('SELECT pg_advisory_unlock(12345)').catch(() => {});
      if (!this.isShuttingDown) {
        this.vacuumTimer = this.scheduleDailyAt(4, 0, () => void this.runVacuumAnalyze());
      }
    }
  }

  private async runReindexConcurrently() {
    // REINDEX CONCURRENTLY는 동시에 두 번 실행되면 에러가 발생하므로
    // advisory lock으로 반드시 단일 실행을 보장한다.
    // listenClient(pg.Client)를 사용해 잠금/해제를 동일 세션에서 수행한다.
    // listenClient가 재연결 중이면 스킵 — 다음 스케줄에 재시도.
    if (!this.listenClient || this.isReconnecting) {
      if (!this.isShuttingDown) {
        this.reindexTimer = this.scheduleWeeklyAt(0, 3, 0, () => void this.runReindexConcurrently());
      }
      return;
    }

    let lockAcquired = false;
    try {
      const result = await this.listenClient.query<{ pg_try_advisory_lock: boolean }>(
        'SELECT pg_try_advisory_lock(12346)',
      );
      lockAcquired = result.rows[0].pg_try_advisory_lock;
    } catch (e) {
      this.logger.warn('[maintenance] advisory lock 확인 실패, REINDEX 스킵');
    }

    if (!lockAcquired) {
      this.logger.log('[maintenance] REINDEX 스킵 (다른 파드가 실행 중)');
      if (!this.isShuttingDown) {
        this.reindexTimer = this.scheduleWeeklyAt(0, 3, 0, () => void this.runReindexConcurrently());
      }
      return;
    }

    try {
      this.logger.log('[maintenance] REINDEX INDEX CONCURRENTLY 시작');
      await this.prisma.$executeRawUnsafe(
        'REINDEX INDEX CONCURRENTLY idx_job_queue_status_created',
      );
      this.logger.log('[maintenance] REINDEX INDEX CONCURRENTLY 완료');
    } catch (e) {
      this.logger.error('[maintenance] REINDEX 실패', String(e));
    } finally {
      await this.listenClient.query('SELECT pg_advisory_unlock(12346)').catch(() => {});
      if (!this.isShuttingDown) {
        this.reindexTimer = this.scheduleWeeklyAt(0, 3, 0, () => void this.runReindexConcurrently());
      }
    }
  }

  // ─── 스케줄러 헬퍼 ───────────────────────────────────────────────────────────

  private scheduleDailyAt(hour: number, minute: number, cb: () => void): ReturnType<typeof setTimeout> {
    const now = new Date();
    const next = new Date(now);
    next.setHours(hour, minute, 0, 0);
    if (next <= now) next.setDate(next.getDate() + 1);
    return setTimeout(cb, next.getTime() - now.getTime());
  }

  private scheduleWeeklyAt(
    dayOfWeek: number,
    hour: number,
    minute: number,
    cb: () => void,
  ): ReturnType<typeof setTimeout> {
    const now = new Date();
    const next = new Date(now);
    next.setHours(hour, minute, 0, 0);
    const daysUntil = (dayOfWeek - now.getDay() + 7) % 7;
    next.setDate(next.getDate() + daysUntil);
    if (next <= now) next.setDate(next.getDate() + 7);
    return setTimeout(cb, next.getTime() - now.getTime());
  }

  // ─── 로그 ────────────────────────────────────────────────────────────────────

  private async safeWriteHandleLog(payload: Record<string, unknown>) {
    try {
      await appendJsonLog('handle.log', payload);
    } catch (e) {
      this.logger.error('handle.log 기록 실패', `${String(e)} / payload: ${JSON.stringify(payload)}`);
    }
  }
}
```

<br />

`queue/queue.module.ts`

```ts
import { Module } from '@nestjs/common';
import { QueueController } from './queue.controller';
import { QueueBuffer } from './queue.buffer';
import { QueueProcessor } from './queue.processor';

@Module({
  controllers: [QueueController],
  providers: [QueueBuffer, QueueProcessor],
})
export class QueueModule {}
```

<br />

`common/append-json-log.ts`

```ts
import { appendFile, mkdir } from 'node:fs/promises';
import { join } from 'node:path';

export async function appendJsonLog(
  fileName: string,
  payload: Record<string, unknown>,
): Promise<void> {
  const now = new Date();
  const y = now.getFullYear();
  const m = String(now.getMonth() + 1).padStart(2, '0');
  const d = String(now.getDate()).padStart(2, '0');
  const dir = join(process.cwd(), 'logs', `${y}`, `${m}`, `${d}`);
  await mkdir(dir, { recursive: true });
  await appendFile(join(dir, fileName), `${JSON.stringify(payload)}\n`, 'utf8');
}
```

<br />

`common/wal-log.ts`

```ts
import { appendFile, readFile, mkdir } from 'node:fs/promises';
import { existsSync } from 'node:fs';
import { join } from 'node:path';
import { randomUUID } from 'node:crypto';
import { EnqueueDto } from '../queue/dto/enqueue.dto';

const WAL_PATH = join(process.cwd(), 'logs', 'wal.log');

interface WalEntry {
  walId: string;
  dto?: EnqueueDto;
  status: 'pending' | 'flushed' | 'compacted';
  timestamp?: string;
}

export async function walAppend(dto: EnqueueDto): Promise<string> {
  const walId = randomUUID();
  const entry: WalEntry = {
    walId,
    dto,
    status: 'pending',
    timestamp: new Date().toISOString(),
  };
  await mkdir(join(process.cwd(), 'logs'), { recursive: true });
  await appendFile(WAL_PATH, JSON.stringify(entry) + '\n', 'utf8');
  return walId;
}

export async function walMarkFlushed(walIds: string[]): Promise<void> {
  if (walIds.length === 0) return;
  const marks = walIds
    .map((id) => JSON.stringify({ walId: id, status: 'flushed' }))
    .join('\n') + '\n';
  await appendFile(WAL_PATH, marks, 'utf8');
}

export async function walReadPending(): Promise<Array<{ walId: string; dto: EnqueueDto }>> {
  if (!existsSync(WAL_PATH)) return [];

  const raw = await readFile(WAL_PATH, 'utf8');
  const lines = raw.split('\n').filter(Boolean);
  const pendingMap = new Map<string, EnqueueDto>();

  for (const line of lines) {
    try {
      const entry = JSON.parse(line) as WalEntry;
      if (entry.status === 'compacted') {
        pendingMap.clear();
      } else if (entry.status === 'pending' && entry.dto) {
        pendingMap.set(entry.walId, entry.dto);
      } else if (entry.status === 'flushed') {
        pendingMap.delete(entry.walId);
      }
    } catch (e) {
      continue;
    }
  }

  return Array.from(pendingMap.entries()).map(([walId, dto]) => ({ walId, dto }));
}

export async function walCleanup(): Promise<void> {
  await mkdir(join(process.cwd(), 'logs'), { recursive: true });
  await appendFile(
    WAL_PATH,
    JSON.stringify({ status: 'compacted', timestamp: new Date().toISOString() }) + '\n',
    'utf8',
  );
}
```

<br />

`common/app-logger.ts`

```ts
import { ConsoleLogger } from '@nestjs/common';
import { appendFile, mkdir } from 'node:fs/promises';
import { join } from 'node:path';

export class AppLogger extends ConsoleLogger {
  private static lastFileErrorAt = 0;
  private static readonly FILE_ERROR_COOLDOWN_MS = 60_000;

  override log(message: string, context?: string) {
    super.log(message, context);
    void this.writeToFile('LOG', message, context);
  }

  override warn(message: string, context?: string) {
    super.warn(message, context);
    void this.writeToFile('WARN', message, context);
  }

  override error(message: string, trace?: string, context?: string) {
    super.error(message, trace, context);
    const body = trace ? `${message}\n${trace}` : message;
    void this.writeToFile('ERROR', body, context);
  }

  override debug(message: string, context?: string) {
    super.debug(message, context);
    void this.writeToFile('DEBUG', message, context);
  }

  override verbose(message: string, context?: string) {
    super.verbose(message, context);
    void this.writeToFile('VERBOSE', message, context);
  }

  private async writeToFile(level: string, message: string, context?: string): Promise<void> {
    try {
      const now = new Date();
      const y = now.getFullYear();
      const m = String(now.getMonth() + 1).padStart(2, '0');
      const d = String(now.getDate()).padStart(2, '0');
      const dir = join(process.cwd(), 'logs', `${y}`, `${m}`, `${d}`);
      await mkdir(dir, { recursive: true });
      const ctx = context ? ` [${context}]` : '';
      const line = `${now.toISOString()} [${level}]${ctx} ${message}\n`;
      await appendFile(join(dir, 'app.log'), line, 'utf8');
      AppLogger.lastFileErrorAt = 0;
    } catch (e) {
      const now = Date.now();
      if (now - AppLogger.lastFileErrorAt > AppLogger.FILE_ERROR_COOLDOWN_MS) {
        AppLogger.lastFileErrorAt = now;
        process.stderr.write(`[AppLogger] app.log 기록 실패: ${String(e)}\n`);
      }
    }
  }
}
```

<br />

`app.module.ts`

```ts
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { AppController } from './app.controller';
import { AppService } from './app.service';
import { AuthModule } from './auth/auth.module';
import { DatabaseModule } from './common/database/database.module';
import { FiltersModule } from './common/filters/filters.module';
import { QueueModule } from './queue/queue.module';
import { MonitorModule } from './monitor/monitor.module';
import { MemberModule } from './member/member.module';
import { ConsultModule } from './consult/consult.module';
import { PartnerModule } from './partner/partner.module';
import { PrivateBoardModule } from './private-board/private-board.module';
import { DailyReportModule } from './daily-report/daily-report.module';

@Module({
  imports: [
    ConfigModule.forRoot(),
    DatabaseModule,
    QueueModule,
    MonitorModule,
    FiltersModule,
    MemberModule,
    AuthModule,
    ConsultModule,
    PartnerModule,
    PrivateBoardModule,
    DailyReportModule,
  ],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

<br />
<br />
<br />

9. 모니터링 대시보드

```
기존 Nest 서버에 모니터링 엔드포인트와 정적 HTML 대시보드를 추가한다.
별도 프로세스 없이 동일 파드 내에서 동작하므로 추가 리소스가 거의 없다.
```

<br />

`k8s/monitor-rbac.yaml`

```yaml
# Nest 파드가 K8s Metrics API를 읽을 수 있도록 권한을 부여한다.
# kubectl apply -f k8s/monitor-rbac.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: monitor-sa
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: monitor-role
rules:
  - apiGroups: [""]
    resources: ["nodes", "pods"]
    verbs: ["get", "list"]
  - apiGroups: ["metrics.k8s.io"]
    resources: ["nodes", "pods"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: monitor-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: monitor-role
subjects:
  - kind: ServiceAccount
    name: monitor-sa
    namespace: default
```

Nest 파드의 Deployment에 ServiceAccount를 연결한다.

```yaml
spec:
  serviceAccountName: monitor-sa
```

<br />

`monitor/monitor.service.ts`

```ts
import { Injectable } from '@nestjs/common';
import * as k8s from '@kubernetes/client-node';
import { PrismaService } from '../prisma.service';

export interface NodeMetric {
  name: string;
  cpuUsage: string;
  memoryUsage: string;
  cpuPercent: number;
  memoryPercent: number;
  status: string;
}

export interface PodMetric {
  name: string;
  namespace: string;
  nodeName: string;
  status: string;
  containers: {
    name: string;
    cpuUsage: string;
    memoryUsage: string;
  }[];
}

export interface QueueStat {
  pending: number;
  processing: number;
  done: number;
  failed: number;
}

@Injectable()
export class MonitorService {
  private metricsClient: k8s.Metrics;
  private coreApi: k8s.CoreV1Api;

  constructor(private readonly prisma: PrismaService) {
    const kc = new k8s.KubeConfig();

    // KUBERNETES_MODE 환경변수로 kubeconfig 로딩 방식을 결정한다.
    // · 'cluster' : 파드 내부 배포 시 — ServiceAccount 토큰 자동 로딩 (운영)
    // · 미설정 또는 그 외 값 : 로컬 개발 시 — ~/.kube/config 로딩
    if (process.env.KUBERNETES_MODE === 'cluster') {
      kc.loadFromCluster();
    } else {
      kc.loadFromDefault();
    }

    this.metricsClient = new k8s.Metrics(kc);
    this.coreApi = kc.makeApiClient(k8s.CoreV1Api);
  }

  async getNodes(): Promise<NodeMetric[]> {
    const [metricsRes, nodesRes] = await Promise.all([
      this.metricsClient.getNodeMetrics(),
      this.coreApi.listNode(),
    ]);

    return metricsRes.items.map((m) => {
      const node = nodesRes.body.items.find((n) => n.metadata?.name === m.metadata.name);
      const readyCond = node?.status?.conditions?.find((c) => c.type === 'Ready');
      const allocCpu = node?.status?.allocatable?.['cpu'] ?? '0';
      const allocMem = node?.status?.allocatable?.['memory'] ?? '0';

      return {
        name: m.metadata.name ?? 'unknown',
        cpuUsage: m.usage.cpu,
        memoryUsage: m.usage.memory,
        cpuPercent: this.cpuPercent(m.usage.cpu, allocCpu),
        memoryPercent: this.memPercent(m.usage.memory, allocMem),
        status: readyCond?.status === 'True' ? 'Ready' : 'NotReady',
      };
    });
  }

  async getPods(namespace?: string): Promise<PodMetric[]> {
    // namespace가 없으면 전체 조회.
    // getPodMetrics에 빈 문자열을 넘기면 잘못된 URL이 생성될 수 있으므로
    // 전체 조회 시에는 listPodForAllNamespaces와 조합해 직접 처리한다.
    const [metricsRes, podsRes] = await Promise.all([
      namespace
        ? this.metricsClient.getPodMetrics(namespace)
        : this.metricsClient.getPodMetrics('default'), // 전체 조회 fallback — 필요 시 확장
      namespace
        ? this.coreApi.listNamespacedPod(namespace)
        : this.coreApi.listPodForAllNamespaces(),
    ]);

    return metricsRes.items.map((m) => {
      const podList =
        'body' in podsRes
          ? (podsRes as any).body.items
          : (podsRes as any).items ?? [];

      const pod = podList.find(
        (p: any) =>
          p.metadata?.name === m.metadata.name &&
          p.metadata?.namespace === m.metadata.namespace,
      );

      return {
        name: m.metadata.name ?? 'unknown',
        namespace: m.metadata.namespace ?? '',
        nodeName: pod?.spec?.nodeName ?? '',
        status: pod?.status?.phase ?? 'Unknown',
        containers: m.containers.map((c) => ({
          name: c.name,
          cpuUsage: c.usage.cpu,
          memoryUsage: c.usage.memory,
        })),
      };
    });
  }

  async getQueueStat(): Promise<QueueStat> {
    // groupBy는 Prisma 5+에서 지원. 하위 버전이면 개별 count 쿼리로 교체한다.
    const groups = await this.prisma.jobQueue.groupBy({
      by: ['status'],
      _count: { status: true },
    });

    const stat: QueueStat = { pending: 0, processing: 0, done: 0, failed: 0 };
    for (const g of groups) {
      const key = g.status as keyof QueueStat;
      if (key in stat) stat[key] = g._count.status;
    }
    return stat;
  }

  private parseCpuMillicores(raw: string): number {
    if (raw.endsWith('m')) return parseInt(raw, 10);
    return parseFloat(raw) * 1000;
  }

  private parseMemBytes(raw: string): number {
    if (raw.endsWith('Ki')) return parseInt(raw) * 1024;
    if (raw.endsWith('Mi')) return parseInt(raw) * 1024 ** 2;
    if (raw.endsWith('Gi')) return parseInt(raw) * 1024 ** 3;
    return parseInt(raw);
  }

  private cpuPercent(usage: string, allocatable: string): number {
    const u = this.parseCpuMillicores(usage);
    const a = this.parseCpuMillicores(allocatable);
    if (a === 0) return 0;
    return Math.round((u / a) * 100);
  }

  private memPercent(usage: string, allocatable: string): number {
    const u = this.parseMemBytes(usage);
    const a = this.parseMemBytes(allocatable);
    if (a === 0) return 0;
    return Math.round((u / a) * 100);
  }
}
```

<br />

`monitor/monitor.controller.ts`

```ts
import { Controller, Get, Query } from '@nestjs/common';
import { MonitorService } from './monitor.service';

@Controller('monitor')
export class MonitorController {
  constructor(private readonly monitorService: MonitorService) {}

  @Get('nodes')
  getNodes() {
    return this.monitorService.getNodes();
  }

  @Get('pods')
  getPods(@Query('namespace') namespace?: string) {
    return this.monitorService.getPods(namespace);
  }

  @Get('queue')
  getQueueStat() {
    return this.monitorService.getQueueStat();
  }
}
```

<br />

`monitor/monitor.module.ts`

```ts
import { Module } from '@nestjs/common';
import { MonitorController } from './monitor.controller';
import { MonitorService } from './monitor.service';

@Module({
  controllers: [MonitorController],
  providers: [MonitorService],
})
export class MonitorModule {}
```

<br />

`public/dashboard.html`

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>K3s Monitor</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #0f1117;
      color: #e2e8f0;
      padding: 24px;
    }
    h1 { font-size: 20px; font-weight: 600; margin-bottom: 4px; }
    .subtitle { font-size: 13px; color: #64748b; margin-bottom: 24px; }
    .grid { display: grid; gap: 16px; }
    .grid-2 { grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); }
    .card {
      background: #1e2130;
      border: 1px solid #2d3148;
      border-radius: 10px;
      padding: 20px;
    }
    .card-title {
      font-size: 13px;
      font-weight: 600;
      color: #94a3b8;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      margin-bottom: 14px;
    }
    .node-item { margin-bottom: 14px; }
    .node-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 6px;
    }
    .node-name { font-size: 14px; font-weight: 500; }
    .badge { font-size: 11px; padding: 2px 8px; border-radius: 12px; font-weight: 600; }
    .badge-ok  { background: #14532d; color: #86efac; }
    .badge-err { background: #7f1d1d; color: #fca5a5; }
    .bar-row { display: flex; align-items: center; gap: 10px; margin-bottom: 4px; }
    .bar-label { font-size: 12px; color: #64748b; width: 40px; }
    .bar-wrap  { flex: 1; background: #2d3148; border-radius: 4px; height: 8px; overflow: hidden; }
    .bar-fill  { height: 100%; border-radius: 4px; transition: width 0.4s ease; }
    .bar-val   { font-size: 12px; color: #94a3b8; width: 56px; text-align: right; }
    .bar-cpu  { background: #3b82f6; }
    .bar-mem  { background: #a855f7; }
    .bar-warn { background: #f59e0b; }
    .bar-crit { background: #ef4444; }
    table { width: 100%; border-collapse: collapse; font-size: 13px; }
    th { text-align: left; padding: 6px 10px; color: #64748b; font-weight: 500; border-bottom: 1px solid #2d3148; }
    td { padding: 8px 10px; border-bottom: 1px solid #1a1f33; color: #cbd5e1; }
    tr:last-child td { border-bottom: none; }
    tr:hover td { background: #252a40; }
    .stat-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }
    .stat-item { text-align: center; }
    .stat-num  { font-size: 28px; font-weight: 700; }
    .stat-lbl  { font-size: 12px; color: #64748b; margin-top: 2px; }
    .c-pending    { color: #60a5fa; }
    .c-processing { color: #facc15; }
    .c-done       { color: #4ade80; }
    .c-failed     { color: #f87171; }
    .refresh-info { font-size: 12px; color: #475569; text-align: right; margin-top: 16px; }
    .dot {
      display: inline-block; width: 7px; height: 7px;
      border-radius: 50%; background: #22c55e; margin-right: 5px;
      animation: pulse 2s infinite;
    }
    @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }
    .error-msg { color: #f87171; font-size: 13px; }
  </style>
</head>
<body>
  <h1>⎈ K3s Monitor</h1>
  <p class="subtitle"><span class="dot"></span>10초마다 자동 갱신</p>

  <div class="grid grid-2">
    <div class="card">
      <div class="card-title">Nodes</div>
      <div id="nodes">로딩 중...</div>
    </div>
    <div class="card">
      <div class="card-title">Job Queue</div>
      <div id="queue">로딩 중...</div>
    </div>
  </div>

  <div class="card" style="margin-top: 16px;">
    <div class="card-title">Pods</div>
    <div id="pods">로딩 중...</div>
  </div>

  <p class="refresh-info" id="last-updated"></p>

  <script>
    const BASE = '';

    function barColor(pct) {
      if (pct >= 85) return 'bar-crit';
      if (pct >= 70) return 'bar-warn';
      return '';
    }

    function renderNodes(nodes) {
      if (!nodes.length) return '<p class="error-msg">노드 데이터 없음</p>';
      return nodes.map(n => `
        <div class="node-item">
          <div class="node-header">
            <span class="node-name">${n.name}</span>
            <span class="badge ${n.status === 'Ready' ? 'badge-ok' : 'badge-err'}">${n.status}</span>
          </div>
          <div class="bar-row">
            <span class="bar-label">CPU</span>
            <div class="bar-wrap">
              <div class="bar-fill bar-cpu ${barColor(n.cpuPercent)}" style="width:${n.cpuPercent}%"></div>
            </div>
            <span class="bar-val">${n.cpuPercent}% <small style="color:#475569">${n.cpuUsage}</small></span>
          </div>
          <div class="bar-row">
            <span class="bar-label">MEM</span>
            <div class="bar-wrap">
              <div class="bar-fill bar-mem ${barColor(n.memoryPercent)}" style="width:${n.memoryPercent}%"></div>
            </div>
            <span class="bar-val">${n.memoryPercent}% <small style="color:#475569">${n.memoryUsage}</small></span>
          </div>
        </div>
      `).join('');
    }

    function renderQueue(q) {
      return `
        <div class="stat-grid">
          <div class="stat-item"><div class="stat-num c-pending">${q.pending}</div><div class="stat-lbl">Pending</div></div>
          <div class="stat-item"><div class="stat-num c-processing">${q.processing}</div><div class="stat-lbl">Processing</div></div>
          <div class="stat-item"><div class="stat-num c-done">${q.done}</div><div class="stat-lbl">Done (24h)</div></div>
          <div class="stat-item"><div class="stat-num c-failed">${q.failed}</div><div class="stat-lbl">Failed</div></div>
        </div>
      `;
    }

    function renderPods(pods) {
      if (!pods.length) return '<p class="error-msg">파드 데이터 없음</p>';
      const rows = pods.map(p => {
        const cpu = p.containers.map(c => c.cpuUsage).join(', ');
        const mem = p.containers.map(c => c.memoryUsage).join(', ');
        const statusColor = p.status === 'Running' ? '#4ade80' : '#f87171';
        return `
          <tr>
            <td>${p.name}</td>
            <td style="color:#64748b">${p.namespace}</td>
            <td style="color:#64748b">${p.nodeName}</td>
            <td style="color:${statusColor}">${p.status}</td>
            <td>${cpu}</td>
            <td>${mem}</td>
          </tr>
        `;
      }).join('');
      return `
        <table>
          <thead>
            <tr><th>Name</th><th>Namespace</th><th>Node</th><th>Status</th><th>CPU</th><th>Memory</th></tr>
          </thead>
          <tbody>${rows}</tbody>
        </table>
      `;
    }

    async function fetchAll() {
      try {
        const [nodes, pods, queue] = await Promise.all([
          fetch(`${BASE}/monitor/nodes`).then(r => r.json()),
          fetch(`${BASE}/monitor/pods`).then(r => r.json()),
          fetch(`${BASE}/monitor/queue`).then(r => r.json()),
        ]);
        document.getElementById('nodes').innerHTML = renderNodes(nodes);
        document.getElementById('pods').innerHTML  = renderPods(pods);
        document.getElementById('queue').innerHTML = renderQueue(queue);
        document.getElementById('last-updated').textContent =
          `마지막 갱신: ${new Date().toLocaleTimeString('ko-KR')}`;
      } catch (e) {
        document.getElementById('nodes').innerHTML =
          `<p class="error-msg">데이터 로드 실패: ${e.message}</p>`;
      }
    }

    fetchAll();
    setInterval(fetchAll, 10_000);
  </script>
</body>
</html>
```

<br />

```
[접근 방법]
· http://<서버IP>:<PORT>/dashboard/dashboard.html

[KUBERNETES_MODE 동작 요약]
· 운영 (파드 내부)  : KUBERNETES_MODE=cluster   → loadFromCluster()
· 로컬 개발        : KUBERNETES_MODE 미설정      → loadFromDefault()
  로컬에서는 ~/.kube/config 가 존재해야 하며,
  k3s 서버의 kubeconfig를 가져온 뒤 server 주소를 실제 IP로 수정해야 한다.

[추가 보안 고려]
· /monitor/* 엔드포인트를 내부 네트워크에서만 접근 가능하도록
  Ingress 또는 NetworkPolicy로 제한하는 것을 권장한다.
· 간단하게는 IP 화이트리스트 가드를 NestJS Guard로 구현할 수 있다.

[대시보드 갱신 주기]
· 브라우저에서 10초마다 /monitor/* API를 자동 호출한다.
· 서버 측 폴링은 없다 — 브라우저 탭이 열려 있을 때만 요청이 발생한다.
· MonitorService는 상태를 유지하지 않는다 (stateless).
  스케일아웃 시 어느 파드에서 응답해도 동일한 데이터를 반환한다.
```

<br />

`대시보드 화면 구성`

| 영역 | 표시 정보 |
| --- | --- |
| Nodes | 노드별 CPU/Memory 사용률(%) + raw 값, Ready 상태 |
| Job Queue | Pending / Processing / Done(24h) / Failed 건수 |
| Pods | 파드명, 네임스페이스, 배치 노드, 상태, 컨테이너별 CPU/Memory |

<br />
<br />
<br />

10. polling vs LISTEN/NOTIFY 비교

| 항목 | polling | LISTEN/NOTIFY |
| --- | --- | --- |
| DB 쿼리 | 주기적으로 고정 발생 | 이벤트 시점에만 발생 |
| 반응 속도 | 주기 지연 발생 | 거의 즉시 반응 |
| 유휴 리소스 | 불필요 소모 | 상대적으로 절약 |
| 구현 난이도 | 단순 | 초기 설정 + 재연결 처리 필요 |
| 신호 누락 대응 | 구조상 누락 없음 | Safety Polling(SAFETY_POLLING_INTERVAL_MS, 기본값 5분)으로 보완 |

<br />
<br />
<br />

11. DB 유지보수 및 정리 전략

| 대상 | 방식 | 주기 |
| --- | --- | --- |
| done 레코드 | 자동 삭제 (24시간 보존) | 1시간마다 |
| failed 레코드 | 수동 확인 후 삭제 | — |
| Dead Tuple | `VACUUM ANALYZE job_queue` | 매일 새벽 4시 (서버 로컬 시간 기준) |
| Index Bloat | `REINDEX INDEX CONCURRENTLY` | 매주 일요일 새벽 3시 (서버 로컬 시간 기준) |

<br />

```
[Dead Tuple (VACUUM ANALYZE)]
· INSERT/DELETE가 반복되면 PostgreSQL MVCC 구조상 삭제된 행이
  즉시 제거되지 않고 Dead Tuple로 남는다.
· autovacuum이 기본 처리하지만 대량 트래픽 시 따라가지 못할 수 있다.
· VACUUM ANALYZE는 Dead Tuple 정리 + 쿼리 플래너 통계 갱신을 동시에 수행한다.
· 실행 중 테이블 잠금 없음 — 운영 중 안전.

[Index Bloat (REINDEX CONCURRENTLY)]
· pending -> done -> DELETE 사이클이 반복되면 인덱스 내부에
  사용되지 않는 페이지(Bloat)가 쌓여 인덱스 스캔 성능이 저하된다.
· REINDEX INDEX CONCURRENTLY는 잠금 없이 백그라운드에서 재구성한다 (PostgreSQL 12+).
· REINDEX INDEX (CONCURRENTLY 없음)는 ShareLock 발생 — 절대 사용하지 않는다.

[실행 시각 기준]
· scheduleDailyAt / scheduleWeeklyAt은 서버 로컬 시간 기준이다.
· TZ=Asia/Seoul 설정 시 한국 표준시(KST) 기준 새벽 4시 / 새벽 3시에 실행된다.
· TZ 미설정 시 UTC 기준으로 동작하므로 배포 환경의 TZ 설정을 반드시 확인한다.
```

<br />

`퇴근 전 확인용 쿼리`

```sql
-- failed 건 확인
SELECT id, type, payload, error, processed_at
FROM job_queue
WHERE status = 'failed'
ORDER BY created_at ASC;

-- 확인 후 개별 삭제
DELETE FROM job_queue
WHERE status = 'failed'
AND id = :id;
```

<br />
<br />
<br />

12. 로그 구조 및 모니터링

```
logs/
└── YYYY/MM/DD/
    ├── app.log      # 전체 서버 로그 (log / warn / error)
    ├── flush.log    # 배치 INSERT 최종 실패 건 (정상 운영에서는 발생하지 않아야 함)
    └── handle.log   # handle() 실패 건 (외부 연동/후속 처리 오류)
logs/
└── wal.log          # enqueue 선기록 (날짜별 아닌 고정 경로 — 재기동 시 복구용)
```

<br />

```
[app.log]
· 전체 서버 로그 (log / warn / error)
· WAL 복구 발생 시 WARN 레벨로 복구 건수가 기록된다
  ("WAL 복구: N건을 버퍼에 재적재합니다.")
· WAL 복구 중 MAX_BUFFER_SIZE 초과 시 WARN 레벨로 초과 건수가 기록된다
  (초과분이 반복되면 MAX_BUFFER_SIZE 상향 또는 인스턴스 증설 검토)

[flush.log]
· 벌크 INSERT 실패 후 단건 INSERT도 실패한 건
· 신규 라인 발생 즉시 알림 권장
· 동일 dbCode / contractNo 반복 실패 시 데이터 품질 이슈 의심

[handle.log]
· QueueProcessor handle() 실패 건
· 1시간 내 누적 N건 초과 시 알림 (N은 운영 합의 필요)
· 동일 jobId 반복 기록 시 멱등성 구현 점검 필요
· 배치 병렬 처리 특성상 동일 배치 내 여러 건이 동시에 기록될 수 있다

[wal.log]
· enqueue 시점에 선기록되는 WAL 파일 (logs/ 루트 고정 경로)
· status: pending — DB에 아직 반영되지 않은 항목
· status: flushed — DB INSERT 성공 후 표시된 항목
· status: compacted — 정상 종료 시 walCleanup()이 기록하는 표식
  이 표식 이전의 모든 항목은 재기동 시 처리 완료로 간주되어 복구 대상에서 제외된다
· 재기동 시 pending 항목이 남아 있으면 WAL 복구 로그가 app.log에 기록됨
· pending 항목이 반복적으로 복구된다면 flush 실패 원인 확인 필요
· 날짜별 디렉터리가 아닌 logs/wal.log 고정 경로 사용
  (재기동 시 날짜가 바뀌어도 동일 파일을 읽어야 복구가 가능하기 때문)
· 스케일아웃 시 WAL 파일은 파드마다 독립적으로 존재한다 (파드 로컬 디스크)
  파드가 교체되면 이전 파드의 WAL은 복구되지 않는다.
  빈번한 파드 교체가 예상된다면 WAL 저장소를 공유 볼륨(PVC)으로 분리하는 것을 검토한다.
· 파일은 append-only로만 기록되므로 장기간 운영 시 크기가 늘어날 수 있다
  재기동(배포) 후 pending 복구 건수가 0이면 파일을 수동으로 비워도 안전하다
```
